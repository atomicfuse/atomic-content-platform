{
  "name": "@atomic-platform/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript interfaces for YAML configs, articles, ads, and tracking",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
