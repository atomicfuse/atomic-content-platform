{
  "name": "atomic-content-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-tenant content network platform \u2014 Astro site builder, Next.js dashboard, AI content pipeline",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@10.30.2",
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@rollup/rollup-darwin-arm64",
      "sharp"
    ]
  }
}